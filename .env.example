# 默认仅本机可访问。通过反向代理上线时保持 127.0.0.1。
QF_BIND_ADDRESS=127.0.0.1
QF_PORT=8080

# 账号、系统令牌和飞书 Webhook 默认在前端「设置」中配置。
# 仅无人值守部署需要使用环境变量覆盖时，取消下面注释并填写；
# 被环境变量覆盖的字段会在前端锁定，不能从页面修改。
# QR_USER_ID=你的账号ID
# QR_SYSTEM_TOKEN=你的系统令牌
# QR_FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/替换为真实地址
